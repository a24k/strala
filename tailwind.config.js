/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // Tailwind v4 uses @theme in CSS instead of JS config for colors
  plugins: []
};